.MessageList{data: {message: {id: message.id}}}
  .MessageHead
    .MessageHead__UserName
      = message.user.name
    .MessageHead__Date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .Message
    - if message.content.present?
      %p.Message__Content
        = message.content
    = image_tag message.image.url, class: 'Message__Image' if message.image.present?
  	